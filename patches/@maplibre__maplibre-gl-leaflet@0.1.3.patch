diff --git a/leaflet-maplibre-gl.js b/leaflet-maplibre-gl.js
index b1bead05c04ad41a9c9255a3b59358fc3fc64dea..731598901b8c1e018411500f0500a844cdcb73e5 100644
--- a/leaflet-maplibre-gl.js
+++ b/leaflet-maplibre-gl.js
@@ -262,6 +262,9 @@
 
         // update the map constantly during a pinch zoom
         _pinchZoom: function (e) {
+            if (!this._map || !this._glMap) {
+                return;
+            }
             this._glMap.jumpTo({
                 zoom: this._map.getZoom() - 1,
                 center: this._map.getCenter()
@@ -271,6 +274,9 @@
         // borrowed from L.ImageOverlay
         // https://github.com/Leaflet/Leaflet/blob/master/src/layer/ImageOverlay.js#L139-L144
         _animateZoom: function (e) {
+            if (!this._map || !this._glMap) {
+                return;
+            }
             var scale = this._map.getZoomScale(e.zoom);
             var padding = this._map.getSize().multiplyBy(this.options.padding * scale);
             var viewHalf = this.getSize()._divideBy(2);
@@ -295,6 +301,9 @@
         },
 
         _zoomEnd: function () {
+            if (!this._map || !this._glMap) {
+                return;
+            }
             var scale = this._map.getZoomScale(this._map.getZoom());
 
             L.DomUtil.setTransform(
@@ -311,6 +320,11 @@
 
         _transitionEnd: function (e) {
             L.Util.requestAnimFrame(function () {
+                // The map/GL layer may have been torn down between scheduling this
+                // animation frame and it firing (e.g. navigating away mid-zoom).
+                if (!this._map || !this._glMap) {
+                    return;
+                }
                 var zoom = this._map.getZoom();
                 var center = this._map.getCenter();
                 var offset = this._map.latLngToContainerPoint(
