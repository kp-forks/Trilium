/** The file names in `packages/share-theme/dist`, listed by `shareThemeAssetListPlugin` in `vite.config.mts`. */
declare module "virtual:share-theme-assets" {
    const files: string[];
    export default files;
}
