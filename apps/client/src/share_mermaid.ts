/**
 * The mermaid the share theme renders diagrams with. Shared pages load it through the manifest
 * `shareMermaidManifest()` writes, so an installation ships one copy of mermaid, not two.
 */
export { default } from "mermaid";
